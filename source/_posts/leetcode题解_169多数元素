## leetcode 169. 多数元素
给定一个大小为 n 的数组，找到其中的多数元素。多数元素是指在数组中出现次数 大于 ⌊ n/2 ⌋ 的元素。[假设数组是非空的，并且给定的数组总是存在多数元素。]

## 三种解法
# 哈希
# 快排
# 摩尔排序（两两配对法）

class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        # # 哈希
        # nums_dic = {}
        # half_nums_length = int(len(nums)/2)
        # for n in nums:
        #     if n not in nums_dic:
        #         nums_dic[n] = 1
        #     else:
        #         nums_dic[n] = nums_dic[n]+1
        #     if nums_dic[n]>half_nums_length:
        #         return n
        # return None
        # # end
        
        # # 排序
        # nums.sort()
        # half_nums_length = int(len(nums)/2)
        # return nums[half_nums_length]
        # # end
        
        
        # 摩尔排序
        cancidate = nums[0]
        caculate = 1
        index = 0
        for n in nums[1:]:  # step2 如果这样写 初始位置在1
            if caculate == 0: 
                cancidate = n # step3 是n不是nums[n]
                caculate = 1
            # if caculate  > half_nums_length:  # stpe1 这个条件错了 这时候判断就已经不是判断 是否大于half了
            #     return cancidate
            else:
                if n==cancidate:
                    caculate += 1
                else:
                    caculate -= 1
            index += 1
        return cancidate
